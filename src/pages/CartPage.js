import React from 'react';
import CartItem from '../components/CartItem/CartItem';

const CartPage = () => {
    return (
        <div>
            <CartItem/>
        </div>
    );
};

export default CartPage;